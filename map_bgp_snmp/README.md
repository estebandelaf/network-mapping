Generación de mapa usando SNMP
==============================

Se utiliza SNMP para obtener la tabla de rutas desde uno o más router 
que están como *looking glass server* y posteriormente se crea el grafo con dot.
